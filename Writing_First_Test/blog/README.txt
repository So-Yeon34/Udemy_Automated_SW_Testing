This is the project for the first automated software test.

Virtual environment
-pip install virtualenv

- virtualenv creates an isolated Python environment for the project.
- It prevents conflicts between different projects' dependencies.
- It ensures the same package versions are used across development and deployment.
- It makes the project easier to share and reproduce on other systems.